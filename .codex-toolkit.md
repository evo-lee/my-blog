# Codex Toolkit Configuration

Project-specific settings for codex-toolkit commands.
Generated by `/init`. Edit freely — all fields are optional.

## Project

- **Stack**: TypeScript, React 19, Vite, Hono, tRPC, Drizzle ORM, Vitest
- **Test command**: `npm test`
- **Source directories**: `src/`, `api/`, `db/`, `scripts/`

## Defaults

These override the built-in defaults for all commands in this project.
Remove a line to fall back to the built-in default.

- **Default model**: gpt-5.5
- **Default effort**: high
- **Default audit type**: mini
- **Default sandbox**: workspace-write

## Audit Focus

balanced

Additional instructions appended to every audit's developer-instructions:

```text
Give equal attention to all audit dimensions.
```

## Skip Patterns

Files and directories to always skip during audits (glob patterns):

```text
node_modules/
dist/
build/
coverage/
*.min.js
*.bundle.js
*.lock
package-lock.json
vendor/
.git/
public/
src/components/ui/
```

## Project-Specific Instructions

Custom instructions appended to Codex's developer-instructions for every command.
Use this to tell Codex about your project's conventions, architecture, or constraints.

```text
This is a full-stack TypeScript app:
- Frontend: React 19 + Vite + TailwindCSS + shadcn/ui (Radix primitives in src/components/ui/)
- Backend: Hono server (api/) with tRPC routers in api/routers/
- Data: Drizzle ORM with better-sqlite3 (db/)
- Auth: DB-backed sessions + bcryptjs + speakeasy TOTP; no JWT/shared secret
- Tests: Vitest (run via `npm test`)
- Type-check: `npm run check` (tsc -b)
- Lint: `npm run lint` (ESLint flat config)

Conventions:
- ESM only (`"type": "module"`)
- Path aliases configured in tsconfig — respect existing import patterns
- Server boots from api/boot.ts, bundled by esbuild during `npm run build`
- shadcn/ui components in src/components/ui/ are vendored — skip during audits
- Treat api/router.ts and api/routers/ as the tRPC procedure source of truth
- API keys are stored as SHA-256 digests; plaintext keys are only shown once
- Site settings live in single-row site_settings (id=1), seeded from db/site-defaults.ts
```
